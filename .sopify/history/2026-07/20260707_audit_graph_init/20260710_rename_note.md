# 2026-07-10 后续更名说明

本方案包记录项目在 2026-07-07 以 `audit-graph` 为名完成初始化时的事实，原有 `background.md`、`design.md` 和 `tasks.md` 保持不变。

2026-07-10，项目正式更名为 `change-audit`：产品、GitHub 仓库、PyPI distribution 与 CLI 使用 `change-audit`，Python import package 使用 `change_audit`，Audit Graph 继续作为内部数据模型名。

更名方案见 `../20260710_change_audit_rename/`。

归档时，初始化范围已经完成；原任务清单第 4 节的后续实现未纳入初始化范围，继续由 `.sopify/blueprint/tasks.md` 跟踪。
