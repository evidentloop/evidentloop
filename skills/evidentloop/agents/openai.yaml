interface:
  display_name: "EvidentLoop"
  short_description: "审计本地 Git 变更，或把人工裁定写回现有报告"
  default_prompt: "Use $evidentloop to review a local Git diff or apply pasted feedback to an existing report."
